_schema-version: "2.0.0"
ID: "GitDemo4"
version: 1.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "GitDemo4"
    type: html5
    path: .
    parameters:
       version: 1.0-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist